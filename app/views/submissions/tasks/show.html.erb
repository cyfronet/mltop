<% breadcrumb :submission_task, @model, @task %>

<%= render "submissions/header", model: @model %>

<%= turbo_stream_from [@model, @task] %>

<% Model::TaskEvaluation.new(model: @model, task: @task).test_set_evaluations.each do |test_set_evaluation| %>
  <div class="rounded-lg border-zinc-200 border mb-12 pt-2">
    <div class="px-4 py-2 sm:px-6">
      <div class="-ml-4 -mt-2 flex flex-wrap items-center justify-between sm:flex-nowrap">
        <div class="ml-4 mt-2">
          <h3 class="text-base font-semibold leading-6"><%= test_set_evaluation.name %></h3>
        </div>
      </div>
    </div>
    <hr class="mx-4">
    <div role="list">
      <%= render test_set_evaluation.hypothesis %>
    </div>
  </div>
<% end %>
