<table class="table-auto border-collapse w-full border border-zinc-400 dark:border-zinc-500 bg-white dark:bg-zinc-800 text-sm shadow-sm">
  <thead class="sticky top-0 bg-zinc-50 dark:bg-zinc-700">
    <tr>
      <th colspan="1" rowspan="2" class="border border-zinc-300 dark:border-zinc-600 font-semibold p-4 text-zinc-900 dark:text-zinc-200 text-left">
        Models
      </th>
      <% task.metrics.each do |metric| %>
        <th rowspan="2" class="border border-zinc-300 dark:border-zinc-600 font-semibold p-4 text-zinc-900 dark:text-zinc-200 text-center">
          <%= link_to test_set_leaderboard_path(test_set, mid: metric.id, o: aggregated_metric_order(metric)), class: "flex items-center justify-center" do %>
            <span class="mr-3"><%= metric.name %></span>
            <%= aggregated_metric_sort_chevron(metric) %>
          <% end %>
        </th>
      <% end %>
      <% task.subtasks.each do |subtask| %>
        <th colspan="<%= task.metrics.size %>" class="border border-zinc-300 dark:border-zinc-600 font-semibold p-4 text-zinc-900 dark:text-zinc-200 text-center font-black">
          <%= subtask.name %>
        </th>
      <% end %>
    </tr>
    <tr>
      <% task.subtasks.each do |subtask| %>
        <% task.metrics.each do |metric| %>
          <th class="border border-zinc-300 dark:border-zinc-600 font-semibold p-4 text-zinc-900 dark:text-zinc-200 text-center">
            <%= link_to test_set_leaderboard_path(test_set, mid: metric.id, sid: subtask.id, o: test_set_metric_order(subtask, metric)), class: "flex items-center justify-center" do %>
              <span class="mr-3"><%= metric.name %></span>
              <%= test_set_sort_chevron(subtask, metric) %>
            <% end %>
          </th>
        <% end %>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% rows.each do |row| %>
      <tr>
        <td class="border border-zinc-300 dark:border-zinc-700 p-4 text-zinc-500 dark:text-zinc-400 font-bold">
          <%= link_to row.name, row.model %>
        </td>

        <% task.metrics.each do |metric| %>
          <%- value = row.score(test_set: test_set, metric:).value %>
          <td class="border border-zinc-300 dark:border-zinc-700 p-4 text-zinc-500 dark:text-zinc-400 text-center"
              style="background-color: <%= interpolate_color(value) %>">
            <%= value %>
          </td>
        <% end %>

        <% task.subtasks.each do |subtask| %>
          <% task.metrics.each do |metric| %>
            <%- value = row.score(test_set: test_set, metric:, subtask:).value %>
            <td class="border border-zinc-300 dark:border-zinc-700 p-4 text-zinc-500 dark:text-zinc-400 text-center"
                style="background-color: <%= interpolate_color(value) %>">
              <%= value %>
            </td>
          <% end %>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
