<h2>Consents</h2>

<%= form.fields_for :agreements do |af| %>
  <% consent = af.object.consent%>
  <div title=<%= consent.required? ? "Required" : "Optional"%> class="consent">
    <p><strong><%= consent.description %> <%= "*" if consent.required?%></strong></p>

    <%= af.hidden_field :agreementable_id %>
    <%= af.hidden_field :agreementable_type %>
    <%= af.hidden_field :consent_id, value: af.object.consent_id %>
    <%= af.hidden_field :id, value: af.object&.id%>
    <%= af.check_box :agreed %>
  </div>
<% end %>
